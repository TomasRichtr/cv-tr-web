/**
 * Enum for data-cy selectors used in tests
 */
export enum Selectors {
  DESCRIPTION = 'description',
  PROFILE_IMAGE = 'profile-image',
  IMAGE_CONTAINER = 'image-container',
}

export const TEST_VALUES = {
  PageTitle: /Tomas Richtr/,
};
