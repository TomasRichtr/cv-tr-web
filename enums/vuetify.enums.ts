export enum Variants {
  Elevated = 'elevated',
  Outlined = 'outlined',
  Flat = 'flat',
  Text = 'text',
  Tonal = 'tonal',
  Plain = 'plain',
}

export enum Density {
  Compact = 'compact',
}

export enum Colors {
  Info = 'info',
  Warning = 'warning',
  Secondary = 'secondary',
  Primary = 'primary',
  Background = 'background',
  Success = 'success',
  Error = 'error',
  Easy = 'easy',
  Amio = 'amio',
  Equiradar = 'equiradar',
}

export enum Sizes {
  XSmall = 'x-small',
  Small = 'small',
  Default = 'default',
  Large = 'large',
  XLarge = 'x-large',
}

export enum Locations {
  Start = 'start',
  End = 'end',
  Left = 'left',
  Right = 'right',
}

export enum AlertTypes {
  Success = 'success',
  Info = 'info',
  Warning = 'warning',
  Error = 'error',
}
